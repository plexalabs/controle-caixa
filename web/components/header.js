// header.js — DEPRECADO no CP5-FIX 2 (substituído por sidebar.js).
//
// O header horizontal antigo foi removido em favor de uma sidebar lateral.
// Este arquivo permanece como stub vazio para não quebrar imports legados;
// será deletado em rodada de polimento futura, quando todas as referências
// a `renderHeader` / `ligarHeader` saírem do código.

export async function renderHeader(_rotaAtiva) {
  return '';
}

export function ligarHeader() {
  // no-op
}
