# =============================================================================
# .env.example — Variáveis de ambiente do Sistema de Controle de Caixa
# =============================================================================
#
# Este arquivo serve como template/documentação. NUNCA preencha valores reais
# aqui. Para uso local:
#   1. Copie este arquivo para `.env` (já bloqueado pelo .gitignore).
#   2. Preencha apenas as variáveis que o seu papel precisa.
#   3. Nunca cometa o `.env` resultante.
#
# Documentação canônica: docs/03_BACKEND_SUPABASE_DATABASE.md §3.
#
# Convenção de papéis:
#   [PUBLIC]   — pode ser exposto no cliente (Excel, Web).
#   [SECRET]   — só backend (Edge Functions, scripts admin).
#   [VAULT]    — armazenado em vault corporativo, nunca em arquivo solto.
# =============================================================================


# --- Supabase --------------------------------------------------------------
# [PUBLIC] URL do projeto. Visível no cliente.
SUPABASE_URL=https://<project-ref>.supabase.co

# [PUBLIC] Chave anônima JWT. Permite operações que passam por RLS.
SUPABASE_ANON_KEY=

# [SECRET] [VAULT] Chave service_role. BYPASSA RLS — usar apenas em Edge
# Functions e scripts administrativos. NUNCA no Excel ou Web.
SUPABASE_SERVICE_ROLE_KEY=

# [VAULT] Senha do banco Postgres. Necessária só para conexão direta via
# psql/pgAdmin. O MCP usa OAuth, não precisa dessa senha.
SUPABASE_DB_PASSWORD=


# --- Frontend (Vite) -------------------------------------------------------
# Variáveis prefixadas com VITE_ são embutidas no bundle do cliente em
# tempo de build. NÃO colocar segredos aqui — apenas valores que JÁ são
# públicos por design (URL de endpoint e anon JWT). Cópia separada das
# acima porque Vite só expõe variáveis com esse prefixo, evitando
# vazamento acidental de SECRET/VAULT.
#
# Coloque os valores reais em .env.local (gitignored) na raiz do projeto.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=

# [PUBLIC] DSN do Sentry — opcional, só é usado em build de produção
# (import.meta.env.PROD === true). Em dev, log.js só usa console. Sentry
# trata DSN como dado público (fica embutido no bundle JS), mas mesmo
# assim vai via env pra ser intercambiável entre projetos. Configurar
# em .env.production local + no painel Cloudflare Pages.
VITE_SENTRY_DSN=


# --- Resend (SMTP do Supabase Auth para envio de OTPs) ---------------------
# [SECRET] Cadastrada em Supabase Dashboard → Auth → Providers → SMTP Settings.
# Procedimento manual em docs/SETUP_RESEND_SMTP.md. NÃO usada pelo código
# do cliente — fica apenas no Dashboard do Supabase.
RESEND_API_KEY=

# Sender configurado no Supabase Auth. Display name + email.
EMAIL_FROM=Caixa Boti <noreply@plexalabs.com>


# --- SMTP genérico (apenas se houver edge function de email transacional) --
# [SECRET] Não usado no MVP — Auth usa Resend acima diretamente.
SMTP_HOST=smtp.resend.com
SMTP_USER=resend
SMTP_PASSWORD=


# --- Criptografia de PII ---------------------------------------------------
# [SECRET] [VAULT] AES-256-GCM. Nunca rotacionar sem migração.
MASTER_ENCRYPTION_KEY=


# --- Cloudflare (deploy via MCP, normalmente não em .env) ------------------
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=


# --- Domínios e URLs (referência) ------------------------------------------
# Usados em Site URL / Redirect URLs do Supabase Auth.
# Produção: PC do trabalho exposto via Cloudflare Tunnel (ver docs/DEPLOY_LOCAL.md).
# Não há mais ambiente Cloudflare Pages.
WEB_URL_PROD=https://caixa-boti.plexalabs.com
WEB_URL_PREVIEW=http://127.0.0.1:4173    # vite preview (build de produção, local)
WEB_URL_LOCAL=http://localhost:5173      # vite dev
